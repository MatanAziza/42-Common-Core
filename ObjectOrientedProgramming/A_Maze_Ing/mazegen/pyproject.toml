[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A simple maze generator module"
authors = [
  { name="Matan Aziza", email="matan.aziza@learner.42.tech" },
  { name="Florian Valota", email="florian.valota@learner.42.tech" },
]
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
